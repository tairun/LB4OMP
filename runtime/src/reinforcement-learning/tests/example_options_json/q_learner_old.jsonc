{
    "name": "q_learner_old", // Options
    "description": "Q-learning algorithm", // Options
    "options": {
        "epsilon": {
            "type": "number",
            "default": 0.1,
            "minimum": 0,
            "maximum": 1,
            "description": "Epsilon-greedy exploration factor"
        },
        "alpha": {
            "type": "number",
            "default": 0.1,
            "minimum": 0,
            "maximum": 1,
            "description": "Learning rate"
        },
        "gamma": {
            "type": "number",
            "default": 0.9,
            "minimum": 0,
            "maximum": 1,
            "description": "Discount factor"
        },
        "episodes": {
            "type": "number",
            "default": 1000,
            "minimum": 0,
            "description": "Number of episodes to run"
        },
        "max_steps": {
            "type": "number",
            "default": 1000,
            "minimum": 0,
            "description": "Maximum number of steps per episode"
        },
        "render": {
            "type": "boolean",
            "default": false,
            "description": "Render the environment"
        }
    },
}